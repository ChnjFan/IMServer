syntax = "proto3";

package IM.BaseType;
option optimize_for = LITE_RUNTIME;

enum MsgType {
    MSG_TYPE_HEARTBEAT = 0;
    MSG_TYPE_LOGIN = 1;
}

enum UserState {
    USER_STATE_ONLINE = 0;
    USER_STATE_OFFLINE = 1;
}

enum ResultType {
    RESULT_TYPE_SUCCESS = 0;
    RESULT_TYPE_NO_SERVER = 1;
    RESULT_TYPE_REQUEST_ERR = 2;
}